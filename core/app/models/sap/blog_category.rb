# -------------------------------------------------------------
# Blog categories model (ex.: news, updates etc)
# -------------------------------------------------------------
class Sap::BlogCategory < ActiveRecord::Base

end